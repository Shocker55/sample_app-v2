<% if @feed_items.any? %>
  <ol class="articles">
    <%= render @feed_items %>
  </ol>
  <%= paginate  @feed_items,
                  params: { controller: :static_pages, action: :home } %>
<% end %>
